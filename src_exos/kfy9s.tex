\titre{}
\theme{derivation}
\auteur{Nathan Scheinmann}
\niveau{3M}
\source{crm}
\type{serie}
\piments{1}
\pts{}
\annee{2526}

\contenu{
\tcblower
Déterminer l'angle entre les graphes de $f$ et $g$ en chacun de leurs points d'intersection.
\begin{tasks}	
\task $f(x)=x^3-4 x$ et $g(x)=x^3-2 x^2$
\task $f(x)=2 x^2-1$ et $g(x)=x^2+2 x+2$
\task $f(x)=\sqrt{3 x^2+1}$ et $g(x)=x+1$
\task $f(x)=\sin (x)$ et $g(x)=\cos (x)$
\task $f(x)=\sin (2 x)$ et $g(x)=\tan (x)$
\end{tasks}
}
\correction{
\tcblower
\begin{tasks}
 \task $75,96^{\circ}$ pour $x=0$ et $6,91^{\circ}$ pour $x=2$
\task $75,96^{\circ}$ pour $x=-1$ et $2,36^{\circ}$ pour $x=3$
\task $45^{\circ}$ pour $x=0$ et $11,31^{\circ}$ pour $x=1$
\task $70,53^{\circ}$ pour $x=\frac{\pi}{4}+k \cdot \pi \quad(k \in \mathbb{Z})$
\task $18,43^{\circ}$ pour $x=k \cdot \pi$ et $63,43^{\circ}$ pour $x= \pm \frac{\pi}{4}+k \cdot \pi$
\end{tasks}
}

