\titre{}
\theme{limites}
\auteur{Nathan Scheinmann}
\niveau{3M}
\source{sesamath3e}
\type{serie}
\piments{2}
\pts{}
\annee{2425}

\contenu{
\tcblower
Calculer les limites suivantes en  justifiant les résultats obtenus. Interpréter graphiquement.
\begin{tasks}(2)
\task $\displaystyle\lim_{x\to +\infty}(x^3 + 2x + 5)$
\task $\displaystyle\lim_{x\to -\infty}(x^4 - 2x)$
\task $\displaystyle\lim_{x\to -\infty}\dfrac{-3}{x^2 + 5}$
\task $\displaystyle\lim_{x\to 0}\Bigl(\dfrac{1}{x^2} + \dfrac{1}{2x^4}\Bigr)$
\task $\displaystyle\lim_{x\to 0}(x^3 + 2x + 5)$
\task $\displaystyle\lim_{x\to -\infty}(x^4 + 2x)$
\task $\displaystyle\lim_{x\to +\infty}(x^3 - x^4)$
\task $\displaystyle\lim_{x\to 0}\dfrac{3x^2 + 5}{1 - 3x^2}$
\task $\displaystyle\lim_{x\to +\infty}\dfrac{x^2 + x + 1}{x}$
\task $\displaystyle\lim_{x\to +\infty}\dfrac{3 - x^2}{x^3}$
\task \(\displaystyle\lim_{x\to-\infty}\bigl(3x-\sqrt{x^2-x+1}\bigr)\)
\end{tasks}
}
\correction{
	\tcblower
	\begin{tasks}(4)
		\task $+\infty$
		\task $+\infty$
		\task $0$
		\task $+\infty$	
		\task $0$
		\task $+\infty$
		\task $-\infty$
		\task $5$
		\task $+\infty$
		\task $0$
\task  $-\infty$

	\end{tasks}

}

